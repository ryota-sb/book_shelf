<div class="unlike-button">
  <%= form_for current_user.likes.find_by(book_id: @book.id),
    html: { method: :delete }, remote: true do |f| %>
    <%= button_tag type: 'submit' do %>
      <i class="fas fa-heart"></i>
    <% end %>
  <% end %>
</div>